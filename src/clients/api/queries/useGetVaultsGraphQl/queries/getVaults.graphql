query getVaults($chainId: Int!, $poolInfosInputs: [poolInfosInput!]!) {
  contracts(chainId: $chainId) {
    xvsVault {
      poolInfos_MULT(args: $poolInfosInputs) {
        token
        allocPoint
        lastRewardBlock
        accRewardPerShare
        lockPeriod
      }
    }
  }
}
